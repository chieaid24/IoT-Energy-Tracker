apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-insight-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: insight-service
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
    protocol: TCP
    name: http
  selector:
    app: insight-service

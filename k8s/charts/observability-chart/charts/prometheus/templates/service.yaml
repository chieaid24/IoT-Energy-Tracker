apiVersion: v1
kind: Service
metadata:
  name: observability-prometheus
spec:
  selector:
    app: observability-prometheus
  ports:
    - name: http
      port: 9090
      targetPort: 9090
  type: LoadBalancer

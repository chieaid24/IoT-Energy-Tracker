# Getting Started

## How to start
Run the docker containers by running this command in the root directory of the project
```
docker compose -v up -d
```

To stop the docker containers, do
```
docker compose down
```